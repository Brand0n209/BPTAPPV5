# Production environment configuration
# This will be used when building the app for deployment

# API URL for production (Google Cloud)
# Note: When deployed to Cloud Run, the server will run on port 8080
# but the URL will be the Cloud Run service URL without a port
REACT_APP_API_URL=https://bright-prodigy-app-923339654929.us-west2.run.app

# Environment indicator
REACT_APP_ENV=production
